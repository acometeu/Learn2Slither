TASK 📝
-mettre le snake avant le board ou trouver une solution quand y'a pas la place
-mettre export path dans le destructor plutot avec un boolean
-verifier que les imports et exports error sont incrashable
-check bucket collision hash table (voir si ameliorer la hash table (voir Boost hash_combine))
-crash en complexstatestrategy board size 3 ou lorsque tableau trop petit (ou c'est juste un throw pas catch)
-un peu mieux ranger les fonctions (dans utils par ex)


Bigger Tasks 🎯
-trouver un moyen pour voir quels fonctions prennent le + de temps et optimiser
-faire Q_reseau de neurones
-1v1 snake and more (1v1v1v1v1v1v1...)
-peut-etre faire des threads
-faire un menu en mode visuel pour choisir des options




little things ❌


-checker si c'est worth de changer std::unordered_map<int> en std::unordered_map<t_coor> avec special hash_map


-gerer les cas de figures ou le serpent commence bloque (pas besoin si tu peux pas changer la taille de base du serpent):
WWWWW       WWWWW
WSSHW       WSS0W
WS0SW       WHS0W
WSSSW       WSS0W
WWWWW       WWWWW


-gerer quand les arguments passes sont des valeurs imposibles (mettre les tailles en unsigned)

-remplacer par des textures si envie

-gerer quand ya plus de places sur le terrain